{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch RAAHAT backend",
      "type": "node",
      "request": "launch",
      "program": "${workspaceFolder}/server.mjs",
      "envFile": "${workspaceFolder}/.env",
      "console": "integratedTerminal"
    }
  ]
}
