# Keep this file local. Never put API keys in app.js or commit .env.
PORT=3000
NODE_ENV=development
# Optional: enables real server-side Responses API calls. Without it, a safe local demo fallback is used.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5
